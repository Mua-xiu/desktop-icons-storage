<Window x:Class="DesktopIconsStorage.App.Views.CloseAskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭 DesktopIconsStorage" Width="430" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource WindowBg}" Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI Variable Text, Microsoft YaHei UI" FontSize="13">
    <StackPanel Margin="24">
        <!-- 标题与说明区 -->
        <TextBlock Text="关闭 DesktopIconsStorage？" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock Text="应用需要在后台运行才能持续显示桌面收纳盒。你可以仅关闭设置窗口并保留系统托盘。"
                   TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}"
                   Margin="0,8,0,18"/>

        <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderSoft}"
                BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,20">
            <CheckBox x:Name="RememberBox" Content="记住我的选择"
                      Foreground="{DynamicResource TextPrimary}"/>
        </Border>

        <!-- 操作区 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="退出应用" Width="108" Margin="0,0,10,0" Click="OnExit"/>
            <Button Content="隐藏到托盘" Width="108" Background="{DynamicResource Accent}"
                    Foreground="White" Click="OnHide"/>
        </StackPanel>
    </StackPanel>
</Window>
