# 收纳筐快捷方式模式 · 需求与可行性方案

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.7 |
| 日期 | 2026-09-24 |
| 状态 | 按评审意见修订，待实现验证 |
| 适用版本 | 现有 v0.3 实体收纳盒基础上新增第二种创建模式 |
| 核心结论 | 新增“快捷方式收纳筐”：源文件始终留在原处；桌面小盒仅预览；点击小盒后在桌面工作区中心打开独立窗口，内容以简短淡入和轻微缩放呈现 |

## 1. 已确认的产品边界

1. 创建时明确选择“实体收纳盒”或“快捷方式收纳筐”，模式写入布局数据，创建后不可切换。两种模式在存储和文件操作上独立，旧盒缺少模式字段时按实体模式读取。
2. 实体收纳盒保留现有真实移动语义。本报告以下“收纳筐”专指新增的快捷方式模式。
3. 向收纳筐拖放、复制粘贴、剪切粘贴文件、文件夹或快捷方式时，收纳筐最终只增加 .lnk，源项目及其路径保持不变。界面不得显示“移动原文件”的效果或提示。
4. 收纳筐始终以紧凑小盒留在桌面。小盒中的图标只是预览，不接受单个图标的打开、选择、拖出或移入其他图标；点击小盒空白或预览区，打开桌面工作区中心的内容窗口。
5. 创建收纳筐时选择预览行列规格，默认 2×2。创建后只能从收纳筐右键菜单修改规格，不允许通过四边或四角拖拽改变小盒尺寸。规格决定预览格数，不限制筐内快捷方式总数。
6. 居中窗口与开合过渡属于首版必需范围，不推迟到第二阶段。拖影实测后改用固定窗口的短暂淡入淡出与轻微缩放，以稳定和清晰为先。
7. 删除快捷方式收纳筐提供两种选择：连同筐内快捷图标一起删除；将快捷图标移至另一个收纳盒后删除当前筐。默认建议前者，避免桌面出现重复图标。内容转移不改变目标盒的模式。
8. 卸载时，快捷方式收纳筐及其受管理的 .lnk 一并删除，不把快捷图标散落到桌面；实体收纳盒中的真实文件仍按原有安全还原规则处理。
9. 新模式小盒的磨砂背景不透明度固定为 10%；展开窗口的磨砂背景不透明度固定为 20%，背景颜色跟随全局深浅主题。这里的百分比指背景叠色的不透明度，数值越低越能透出桌面，不是整个窗口及图标、文字的不透明度。
10. 小盒预览始终只显示图标，不显示名称；展开窗口右上角提供“显示名称”开关。关闭后，展开窗口内的图标在鼠标悬停时用跟随深浅主题的提示层显示完整名称。

## 2. 问题与方案

实体收纳盒的真实移动会改变项目路径。跨卷移动大型目录还会产生大量复制 IO。快捷方式收纳筐在真实目录内保存 Windows .lnk，指向原始项目，解决这两个问题。项目原本采用“盒对应真实文件夹”的结构，图标提取、Shell 打开和文件夹监听可继续利用，但输入、删除、窗口和卸载流程必须按模式重做，不能只修改拖入入口。

收纳筐本身是引用容器，不是系统桌面图标的自动分组。拖入桌面项目后，原桌面图标仍存在；用户可按需手动隐藏系统桌面图标，但那是全局设置，会影响所有桌面图标。首版不自动切换这个系统设置。

## 3. 创建、持久化与目录

### 3.1 创建入口

托盘“新建收纳盒”打开创建界面，填写名称并选择模式。选择快捷方式收纳筐时显示预览规格设置，默认 2 行 × 2 列，行数和列数均可在 2～10 之间独立选择。创建界面明确告知“只生成快捷方式，原文件保留原位”。模式提交后不可修改；右键菜单可重命名，也可打开规格弹窗重新选择行列。大规格须按显示器工作区等比缩小，避免小盒越屏。

### 3.2 模型与兼容

在 Core 的 Block 模型中新增受约束的 Mode（move/link）、PreviewRows、PreviewColumns。旧布局缺少 Mode 时默认 move；旧实体盒不因新增字段改变尺寸或交互。读取未知 Mode 时不得猜测文件操作方式，应阻止涉及文件的动作并提示配置异常。行列值读取时做范围校验，避免异常配置撑出屏幕。

建议继续使用 StorageRoot 下的平铺盒目录，以减少改动。不要在首版新增 Links 中间目录：当前重命名固定使用 StorageRoot\<名称>，卸载也只处理盒目录和根目录；嵌套目录会让现有逻辑不一致。模式由布局字段识别，不依赖目录名称。创建时仍检查两个模式之间的同名目录冲突。

### 3.3 目录内容约束

链接筐由应用创建的内容只应是 .lnk。由于用户可以通过资源管理器进入真实目录，目录中可能出现普通文件或子目录。清理、删除和卸载不能假定其中全是快捷方式，更不能递归删除未知内容。发现非 .lnk 时应停止清空和移除目录，保留原内容并向用户说明具体目录；不得把异常内容当作链接目标删除。

## 4. 输入与跨盒操作语义

| 来源与操作 | 收纳筐结果 | 来源结果 |
|---|---|---|
| 拖入真实文件或文件夹 | 新建指向该路径的 .lnk | 原文件或目录不动 |
| 复制后粘贴真实项目 | 新建指向该路径的 .lnk | 原项目不动 |
| 剪切后粘贴真实项目 | 同样新建 .lnk，按“添加引用”处理 | 原项目不动；不执行剪切后的源删除 |
| 拖入或粘贴已有 .lnk | 复制该 .lnk 到筐内；不制作“快捷方式的快捷方式” | 原 .lnk 不动 |
| 从其他链接筐拖入或粘贴 .lnk | 复制 .lnk，原筐保留；删除筐时的“转移”是另一个明确动作 | 原筐不动 |
| 从实体盒拖入真实项目 | 新建指向实体盒内现有路径的 .lnk | 实体盒内原项目不动；盒模式不变 |
| 对链接筐中的项目排序 | 只调整显示顺序 | 文件和链接目标不动 |

文件夹本身也以 .lnk 表示。收纳筐的预览图标与展开窗口的图标均不提供“拖到另一个图标中央，移入该文件夹”的能力；不能调用现有 MoveIntoFolder。展开窗口允许操作筐内快捷方式本身，例如打开、排序、拖出、删除和系统右键菜单。拖出到桌面或资源管理器时传递 .lnk 文件，不解析为目标路径；这个动作可能移动快捷方式，但不得移动其目标。链接筐项目拖入实体盒时，实体盒按现有语义移动 .lnk 本身，快捷方式目标不动；这是内容转移，不是模式切换。若用户从资源管理器自行操作 .lnk，仍遵循系统文件行为。

接收拖放时不能继续固定报告 Move。链接筐应声明创建引用或复制快捷方式的效果，并对资源管理器、第三方文件管理器和本应用内部拖放做实测，确认拖放源不会因回报效果而删除原项目。应用收到 FileDrop 路径后也要拒绝空路径、不存在项目和将筐自身目录拖入自身等循环引用。批量处理中逐项记录成功、失败和冲突，失败不得导致源文件丢失。

同名快捷方式使用序号避免覆盖。.lnk 文件名从源显示名生成；复制已有 .lnk 时保留原文件名并解决冲突。生成快捷方式不附带命令行参数或远程图标地址。不可访问的网络路径、系统虚拟图标和未提供文件路径的拖放数据先提示不支持，不能冒充成功。

### 4.1 实现分层

Core 保留模式、目录、命名和操作规则；Platform.Windows 提供创建/解析 .lnk 的 ShellLinkService；App 层负责按模式编排拖放与剪贴板。若要由 Core 发起链接创建，应先在 Core 定义接口并由 Platform.Windows 注入，不能让 Core 反向引用 Windows 平台项目。拖放与粘贴必须调用同一条“添加到链接筐”业务入口，避免再次出现某个入口误走 MoveInto 或 CopyInto。

## 5. 小盒、居中窗口与动画

### 5.1 小盒预览

小盒按所选行列展示前 N 个快捷方式图标，N = 行数 × 列数；超出容量时显示剩余数量，完整内容只在弹窗中查看。图标按保存的手动顺序选取。小盒预览始终不绘制图标名称，也不在单个预览图标上弹出名称提示；名称只在展开窗口中提供。小盒整个可见区域都可拖动位置；未发生拖动的点击打开收纳筐，单个预览图标不接收打开、选中、拖出或文件夹热区事件。小盒接受外部文件拖入；四边及四角缩放手柄在链接模式下移除或禁用。右键“设置预览规格…”打开独立弹窗，分别选择 2～10 行和列，保存后按 DPI 与工作区重新计算小盒尺寸。

小盒使用固定 10% 不透明度的半透明磨砂背景，背景叠色 alpha 为 0.1，桌面应明显可见；不提供透明度调节入口，也不读取全局“背景不透明度”滑块。该数值只作用于背景叠色，不降低图标、标题和边框自身的不透明度；这些内容在浅色和深色桌面上仍须可辨认。

### 5.2 居中内容窗口

点击小盒后，在该小盒所在显示器的工作区中心打开独立内容窗口，避免跨显示器或被任务栏遮挡。常规目标尺寸为 780×560 DIP，按所属显示器 DPI 换算并限制在工作区内。窗口展示全部快捷方式，图标可打开对应目标。容量按当前窗口尺寸计算；内容超过单页容量时分页显示，不出现滚动条。页点位于窗口底部中央，点击圆点或使用左右方向键切页；只有一页时隐藏圆点。窗口右上角不放关闭按钮，由 Esc 或点击窗口外区域收起；收起后小盒保持原位置和规格。

展开窗口右上角提供与设置窗口一致的滑动开关样式“显示名称”，不使用勾选框外观。它只控制该收纳筐展开窗口内的图标文字，小盒预览始终不显示名称。初次使用沿用现有全局显示名称设置；用户切换后复用 Block.ShowIconNames 记录该筐的覆盖值，关闭并重新打开窗口时须立即按保存值恢复文字可见性。开关切换应立即更新图标排布，不改变快捷方式或预览规格。

关闭“显示名称”时，鼠标悬停在展开窗口的图标上，应浮出该项目的完整显示名称；移开后消失，不显示被截断的名称，也不暴露仅用于存储的 .lnk 扩展名。提示层的背景、文字、边框跟随当前深浅主题，并在系统主题或应用主题切换时同步更新；浅色和深色桌面背景下均须清晰可读。开启“显示名称”时不再重复显示同一名称提示。

展开窗口使用与小盒分开的固定 20% 不透明度半透明磨砂背景，背景叠色 alpha 为 0.2，桌面仍应明显可见。背景颜色跟随全局的系统/浅色/深色主题选择及主题变化，但背景不透明度不受全局“背景不透明度”滑块控制，也不提供按筐调节入口。该数值只作用于背景叠色，不使图标、文字和操作按钮一起变淡。主题切换时应同步更新已打开的窗口。

当布局中存在快捷方式收纳筐时，设置页禁用全局图标大小选项并解释原因；链接筐的图标尺寸固定，确保分页容量可预测。删除所有链接筐后恢复该选项。

### 5.3 定点开合过渡

实际使用录屏显示，逐帧移动和缩放原生毛玻璃窗口会在桌面留下旧轮廓拖影。窗口现固定在所属显示器中心，以最终尺寸一次创建；小盒留在原位，展开内容约 180ms 淡入并从 97% 轻微放大至 100%，收起内容约 140ms 淡出并轻微缩小后销毁窗口。原生毛玻璃窗口不参与逐帧位移或缩放，也不再拉伸小盒截图。动画期间禁止重复打开、重复关闭和拖放操作；中途收到 Esc、显示器变化或 Explorer 重建时，要安全结束并恢复小盒可交互状态。

首版目标是无拖影、不卡顿和视觉轻盈。透明度与缩放由 WPF 动画完成，不得为追求特效阻塞 UI 线程。高 DPI、多显示器、不同刷新率和系统减少动画偏好需要验证；减少动画开启时直接切换，仍保持操作可用。

### 5.4 先行可行性验证与停止条件

正式改造文件操作和持久化前，先做可丢弃的最小窗口原型，验证：桌面小盒点击后在所属显示器工作区中心出现窗口；正确处理桌面层级、焦点、窗口外关闭；定点开合动画无明显拖影；小盒 10%、展开窗口 20% 的背景不透明度能分别呈现，图标和文字仍清晰，主题切换不改变这两个固定数值；在高 DPI、双屏和 Explorer 重启后可恢复。现有 BlockWindow 只会原位改变高度，没有独立居中弹窗；现有背景不透明度滑块最低为 30%，也不能直接提供这两档效果，因此这一步不能跳过。

如果窗口层级、焦点、定点开合过渡或这两档固定磨砂效果在当前 Windows 桌面宿主机制下无法稳定达到上述功能要求，应停止该模式的后续开发，记录复现环境、失败原因与可行替代方案，交由用户决定。不得未经确认退回“原位展开网格”或其他背景效果并声称完成新模式。

## 6. 删除、还原与卸载

### 6.1 删除单个快捷方式

在展开窗口删除单个项目，只对选中的筐内 .lnk 执行回收站删除。删除前确认路径位于当前筐目录且扩展名为 .lnk；绝不删除 .lnk 指向的源文件。系统右键菜单针对快捷方式本身，若扩展菜单引发异常，刷新后仍以实际文件状态为准。

### 6.2 删除整个链接筐

删除对话框在链接模式下只提供两项：

1. “删除收纳筐及其中的快捷方式”：将受管理的 .lnk 移入回收站，然后仅在目录确实为空时删除目录和布局记录。不把快捷方式移回桌面。
2. “将快捷方式移到其他收纳盒并删除当前筐”：目标列表显示其他收纳盒，不改变目标盒模式。逐个移动 .lnk 文件，处理同名冲突；即使目标是实体盒，也只移动快捷方式文件，不触碰其目标。全部成功且源目录为空后才删除当前筐及布局记录。部分失败时保留当前筐和未转移内容，显示失败信息。

不能沿用当前“删除异常后仍关闭窗口”的行为。删除成功后才移除窗口与模型。若筐内有非 .lnk、隐藏文件或其他未识别内容，停止删除并保留目录，提示用户处理；不得以递归删除代替检查。

### 6.3 一键还原与卸载

“一键全部还原”在两种模式下有不同结果：实体盒的真实文件移回桌面；链接筐的 .lnk 清空到回收站，源项目始终留在原位，筐本身保留。确认框应分模式列出数量和结果，不能再统一写“所有文件移回桌面”。

卸载器清理链接筐时，删除受管理的 .lnk 和确认为空的链接筐目录，不在桌面生成快捷方式；实体盒真实文件仍移回桌面。全部盒目录为空后再删除 StorageRoot。若任何目录含未知内容或删除失败，保留目录和配置并向卸载器返回失败，防止用户数据被误删或失去定位信息。卸载清理通常不可依赖回收站，需在卸载确认文案中明确“快捷方式会删除，目标文件不会删除”。

## 7. 异常、可用性与安全约束

- 目标被移动或删除时，.lnk 可能失效。可在展开窗口标记无法解析或无法访问的快捷方式，但不能把 File.Exists == false 直接当作永久失效的唯一依据；网络盘离线与 Shell 虚拟目标需区分。清理失效链接必须由用户触发。
- Shell 的链接追踪能力只能作为辅助，不保证所有目标移动后自动修复。验收需覆盖本机重命名、跨卷移动、网络路径暂时不可用和目标删除。
- 若快捷方式指向实体盒内的项目，实体盒以后按原有模式移动或还原该项目时，链接目标路径可能变化；界面应说明此关联风险，刷新时按失效链接处理，不能承诺永久有效。
- 大批量创建 .lnk 与图标提取要保持界面响应。失败项目单独提示，已成功创建的链接可以保留；不能在半途将任何源项目移动或删除。
- 隐藏系统桌面图标只作为手动引导，不纳入首版程序开关。提示该设置影响全部桌面图标，并确保用户知道原项目仍在原位置。
- 对现有实体盒的拖放、缩放、删除、卸载和布局兼容不得产生回归。

## 8. 首版实现顺序与验收

顺序仅表示开发中的风险验证先后，不代表分期交付：

1. 独立窗口与双向动画原型；未通过第 5.4 节停止条件则停止功能开发。
2. 固定模式、规格及旧布局兼容；创建界面与链接筐小盒。
3. ShellLinkService 和统一添加入口；覆盖拖放、复制粘贴、剪切粘贴及内部跨筐操作。
4. 展开窗口的图标交互、排序、单项删除、右上角名称开关及主题化悬浮提示，以及小盒预览只读限制。
5. 删除筐、转移至其他收纳盒、一键还原与卸载清理。
6. 多显示器、高 DPI、Explorer 重启、异常文件、批量输入和实体盒回归验证。

首版必须同时满足：模式创建后不可切换；小盒默认 2×2，创建和右键规格弹窗可分别选择 2～10 行与列；小盒只预览对应规格数量的图标、不滚动、不显示图标名称且不可直接打开图标；小盒磨砂背景固定 10% 不透明度、展开窗口固定 20% 且跟随全局深浅主题；点击小盒在所属屏幕中心打开内容窗口；展开窗口按容量分页，页点及键盘左右键均可切页，单页隐藏页点；名称开关即时生效并持久化，关闭名称后悬停图标能显示随主题变化的完整名称；实体盒最小高度允许一行；定点开合过渡没有明显拖影；所有输入只增加快捷方式而不移动源项目；删除与卸载只清理快捷方式及空目录；异常情况下不误删原文件。任何一项缺失都不能以“后续阶段补充”作为首版完成标准。

原文“链接模式 3.5–5 人日”只覆盖部分文件语义，未计入独立窗口、双向动画、焦点与多屏验证及完整删除流程，现已失效。应在窗口原型通过后重新估算，不沿用该数字。

## 9. 关键代码落点

| 现有位置 | 需要调整的原因 |
|---|---|
| src/Core/Models/Block.cs | 保存固定模式与预览规格；复用 ShowIconNames 保存展开窗口的名称显示覆盖值，兼容旧布局 |
| src/Core/Services/BlockManager.cs | 模式化创建、命名、枚举、删除和还原；禁止链接模式误走真实移动 |
| src/Platform.Windows/Services/ | 增加 ShellLinkService；提供快捷方式创建与解析 |
| src/App/Helpers/AppHost.cs | 统一输入编排、创建、删除、还原和卸载；只有成功后移除窗口 |
| src/App/Views/BlockView.xaml.cs | 链接模式预览只读；拖放效果、剪贴板、子文件夹热区及删除语义分派 |
| src/App/BlockWindow.cs 与新弹窗视图 | 小盒几何、独立居中窗口、双向过渡与焦点管理；弹窗名称开关和随主题变化的悬浮提示 |
| src/App/Views/DeleteBlockDialog.* | 链接模式的两种删除选择及目标筐过滤 |
| src/App/Tray/TrayManager.cs | 创建入口与一键还原文案 |
| src/Platform.Windows/Services/DesktopEmbedService.cs | 验证居中弹窗的桌面层级、多屏坐标与窗口外关闭 |
